{
  "name": "whisper",
  "version": "0.3.0",
  "description": "A website to secretly share secrets or passwords temporarily",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "install:all": "npm install --prefix client && npm install --prefix api"
  },
  "keywords": ["secret", "sharing", "encryption", "privacy"],
  "author": "Andri Petur",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/andripetur/Whisper.git"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
